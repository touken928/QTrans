# Ensure consistent line endings across platforms so that
# GitHub Actions' hashFiles() produces identical cache keys.
* text=auto eol=lf

# Explicit binary files
*.png binary
*.jpg binary
*.ico binary
*.gguf binary
*.zip binary
*.tar.gz binary
